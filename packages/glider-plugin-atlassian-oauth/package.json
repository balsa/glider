{
  "name": "@glider/plugin-atlassian-oauth",
  "version": "0.1.0",
  "private": true,
  "description": "Glider plugin for authenticating with Atlassian services using OAuth",
  "author": "Balsa, Inc.",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build:base -- --sourcemap",
    "build:base": "esbuild ./src/index.ts --bundle --outfile=dist/main.js --external:glider --format=cjs --platform=node",
    "build:release": "npm run build:base -- --minify",
    "build:watch": "npm run build:base -- --sourcemap --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/glider": "*"
  }
}
